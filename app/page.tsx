import React, { useState } from 'react';\n\nconst RoleSelectorModal = ({ isOpen, onClose }) => {\n    const [selectedRole, setSelectedRole] = useState('');\n    const roles = ['Admin', 'User', 'Viewer'];\n\n    const handleRoleSelection = (role) => {\n        setSelectedRole(role);\n    };\n\n    const handleSubmit = () => {\n        // Perform action based on selected role\n        console.log(`Selected Role: ${selectedRole}`);\n        onClose();\n    };\n\n    if (!isOpen) return null;\n\n    return (\n        <div className="modal">\n            <h2>Select Your Role</h2>\n            {roles.map((role) => (\n                <button key={role} onClick={() => handleRoleSelection(role)}>{role}</button>\n            ))} \n            <button onClick={handleSubmit}>Submit</button>\n            <button onClick={onClose}>Close</button>\n        </div>\n    );\n};\n\nconst App = () => {\n    const [isModalOpen, setIsModalOpen] = useState(false);\n\n    return (\n        <div className="app">\n            <h1>Landing Page</h1>\n            <button onClick={() => setIsModalOpen(true)}>Select Role</button>\n            <RoleSelectorModal isOpen={isModalOpen} onClose={() => setIsModalOpen(false)} />\n        </div>\n    );\n};\n\nexport default App;